<%= form_with model: Song.new do |f| %>

  <div>
    <%= f.collection_select :genre_id, @genres, :id, :name, { prompt: 'Select Genre' } %>
  </div>

  <div>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <%= f.submit %>

<% end %>
